Day 4 — DOM Manipulation & Events 
1. Selecting Elements * getElementById() * getElementsByClassName() * getElementsByTagName() * querySelector() * querySelectorAll() 

2. Changing Content * textContent * innerHTML 

3. Changing Attributes * getAttribute() * setAttribute() * removeAttribute() 

4. Event Listeners (CRITICAL - Practice extensively) * addEventListener() * Common events: click, submit, input, change * event.preventDefault() * Event object basics 

5. Changing Styles * Inline styles via element.style.property * CSS classes via classList.add(), classList.remove(), classList.toggle() 

6. Creating and Removing Elements * createElement() * appendChild() * append() * prepend() * removeChild() * remove() 

7. DOM Traversal (Recommended but optional if time is limited) * parentElement * children * nextElementSibling * previousElementSibling 

8. Hands-on Mini-Projects (ESSENTIAL) * Create a button that adds items to a list * Build a simple form that displays user input * Make elements change color/style on click * Integrate multiple concepts in one small project Focus on practical exercises for each section.